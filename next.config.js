/** @type {import('next').NextConfig} */
const nextConfig = {
  reactStrictMode: true,
  // Next.js 14+ enables swcMinify by default
  // We're using the app directory, so we don't need experimental features anymore
  // as they're now stable in Next.js 14+
};

module.exports = nextConfig;
